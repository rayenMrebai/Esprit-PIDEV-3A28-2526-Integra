<form method="post" action="{{ path('app_user_account_delete', {'userId': user_account.userId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_account.userId) }}">
    <button class="btn">Delete</button>
</form>
