<form method="post" action="{{ path('app_projectassignment_delete', {'idAssignment': projectassignment.idAssignment}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projectassignment.idAssignment) }}">
    <button class="btn">Delete</button>
</form>
